单链表：
    1、奇数个数据返回唯一中点，
    2、偶数返回下一个中点；
    3、奇数个数量，返回中点前一个中点；
    4、偶数个数量，返回上中点。
    5、判断是否为回文：（两种方法：1、容器法（栈），2、调整生成尾节点）
    6、给定标准值，分隔单链表（小于部分+等于部分+大于部分）
    7、复制一个包含random指针的单链表（1、使用容器；2、不使用容器）
    8、确认单向链表入环节点（若无环，返回null,有环正常返回），方法（1、容器法；2、fast、slow指针法）
    9、确定两个单向链表是否相交（第八条基础上：1、容器法，2、不用容器；情况：都无环，都有环，不可能存在一个有环一个无环的情况）
    10、约瑟夫问题（单链表）---待完成