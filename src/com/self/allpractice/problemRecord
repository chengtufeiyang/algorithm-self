1、左右括号四连问题（字符串中包含"("")"两种不同类型字符）
    a、判断字符串是否满足括号成对出现的情况（不符合要求情况：")("，符合要求情况"(())"）
    b、给定字符串判断需要补充多少字符可，将字符串补全为符合左右括号对应字符串
    c、求取字符串中符合左右括号对应最大长度子串
    d、求最大有效嵌套层
2、给定M*N矩阵中，值均为0  or  1,求取该矩阵中边长均为1的最大正方形边长
3、一列长方形中，仅存在两种颜色（RG）,求使用最小代价，会形成一个虚拟分割线，左侧均为R,右侧均为G
4、给定数组长度M，生成符合满足以下条件的数组arr,下标顺序：i < j < k ，生成符合 arr[i] + arr[k]  != 2 * arr[j] 的数组

5、二叉树中某个起点到某终点的最长路径和（每个节点上值的累加），分为以下四种情况
	起点           终点
	开始节点       叶子节点
	任意节点       任意节点（只可向下）
	任意节点       叶子节点
	任意节点       任意节点

6、确定特定二维数组中是否存在目标数，该二维数组具有以下特征，左->右：数据逐渐增加
                                                          上->下：数据逐渐增加


7、有n个打包机器从左到右一字排开，上方有一个自动装置会抓取一批放物品到每个打 包机上，放到每个机器上的这些物品数量有多有少，由于物品数量不相同，
需要工人 将每个机器上的物品进行移动从而到达物品数量相等才能打包。每个物品重量太大、 每次只能搬一个物品进行移动，为了省力，只在相邻的机器上移动。
请计算在搬动最 小轮数的前提下，使每个机器上的物品数量相等。如果不能使每个机器上的物品相同， 返回-1。 例如[1,0,5]表示有3个机器，每个机器上分别有1、0、5个物品，经过这些轮后:
  第一轮:1    0 <- 5 => 1 1 4 第二轮:1 <- 1 <- 4 => 2 1 3 第三轮:2    1 <- 3 => 2 2 2
  移动了3轮，每个机器上的物品相等，所以返回3
  例如[2,2,3]表示有3个机器，每个机器上分别有2、2、3个物品， 这些物品不管怎么移动，都不能使三个机器上物品数量相等，返回-1

类似洗衣机调度问题，使得所有机器操作相同数量的衣服（核心点在于，分为三个部分：
	左侧部分累加和，当前位置值，右侧部分累加和）


8、任意排序数组中，任意分割两部分请款下，左右两侧部分中最大值差值的最大值


9、一维数组arr中寻找目标数值target
	二元数组满足target所有结果（no repeat）
	三元数组满足target所有结果（no repeat）

10、数组蓄水问题（接水问题。。。）
	一维数据求解
	二维数组求解

11、数组arr,数值k,返回数组所有数值对第k小数（例如：数组arr:[1,2],数值对：{(1,1),(1,2),(2,1),(2,2)}）